{
  "package": "example",
  "engines": {
    "node": "18 || 20 || 21 || 22"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.3"
  },
  "napi": {
    "name": "example"
  },
  "scripts": {
    "build": "napi build --release",
    "build:debug": "napi build"
  }
}
